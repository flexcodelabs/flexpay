import {
  ApiKey,
  Channel,
  ChannelProperty,
  CoreChannel,
  Metadata,
  User,
} from '@flexpay/common';

export const authEntities = [
  User,
  ApiKey,
  Channel,
  Metadata,
  ChannelProperty,
  CoreChannel,
];
