import {
  ApiKey,
  Channel,
  ChannelValue,
  CoreChannel,
  Metadata,
  User,
} from '@flexpay/common';

export const authEntities = [
  User,
  ApiKey,
  Channel,
  Metadata,
  ChannelValue,
  CoreChannel,
];
