import {
  ApiKey,
  Channel,
  ChannelKey,
  CoreChannel,
  Metadata,
  User,
} from '@flexpay/common';

export const authEntities = [
  User,
  ApiKey,
  Channel,
  Metadata,
  ChannelKey,
  CoreChannel,
];
